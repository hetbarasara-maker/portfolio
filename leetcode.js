const get = async(user) => { const r = await fetch('https://leetcode.com/graphql', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ query: 'query { matchedUser(username: \u0022'+user+'\u0022) { submitStats { acSubmissionNum { difficulty count } } } }' }) }); const j = await r.json(); console.log(user, JSON.stringify(j.data.matchedUser.submitStats.acSubmissionNum)); }; get('Het_Barasara'); get('neetcode');
